version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: concerts_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: concerts
    ports:
      - "5432:5432"